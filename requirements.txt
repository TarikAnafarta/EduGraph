# EduGraph - Backend (Django + DRF)
# This requirements.txt only applies to the Python backend.
# The React frontend now lives in frontend/ and is managed with Node (npm/yarn).

# Django and REST Framework for API
Django>=4.2.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
django-extensions>=3.2.0
django-cloudinary-storage>=0.3.0
cloudinary

python-dotenv

# Database
psycopg2-binary>=2.9.0    # PostgreSQL adapter for Python

# Additional dependencies for user management
python-dateutil>=2.8.0
requests>=2.31.0
python-decouple>=3.8

# Development dependencies (uncomment if needed):
# pytest>=7.4.0         # For testing
# black>=23.9.0         # Code formatting
# flake8>=6.1.0         # Linting
# mypy>=1.6.0           # Type checking

# Notes:
# - Static files for Django admin and apps are still collected via collectstatic.
# - Frontend bundling (Vite) and dependencies are not listed here; see frontend/package.json.
